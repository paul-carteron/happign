# Print latest news from geoservice website

This function is a wrapper around the RSS feed of the geoservice site to
get the latest information.

## Usage

``` r
get_last_news()
```

## Value

message or error

## Examples

``` r
if (FALSE) { # \dontrun{
get_last_news()
} # }
```
